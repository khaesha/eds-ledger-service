# Test Environment Configuration
NODE_ENV=test

# Database
DATABASE_URL=postgresql://test:test@localhost:5432/eds_ledger_test

# JWT
JWT_SECRET=test-jwt-secret-key-do-not-use-in-production

# OpenRouter API (mocked in tests - dummy value)
OPENROUTER_API_KEY=test-openrouter-key-123

# Throttle (rate limiting)
THROTTLE_TTL=900000
THROTTLE_LIMIT=5
